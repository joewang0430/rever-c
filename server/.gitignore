
# -------------------------------------------------------------------
# === Python Virtual Environment ===
# Ignore the entire .venv folder to avoid committing third-party packages to the repository
.venv/

# -------------------------------------------------------------------
# === Python Cache Files / Bytecode ===
__pycache__/
*.py[cod]
*$py.class

# -------------------------------------------------------------------
# === Environment Variable Config Files ===
# If you use .env to store sensitive configurations (e.g., database passwords), ignore it
.env

# -------------------------------------------------------------------
# === Shared Libraries / Build Artifacts ===
# Ignore all compiled binary files like .so, .dylib, .dll, etc.
*.so
*.dylib
*.dll

# -------------------------------------------------------------------
# the archived algorithms are blocked to protect the intellectual property
data/c_src/archives/*
data/c_src/candidates/*
data/c_src/caches/*

!data/c_src/archives/.gitkeep
!data/c_src/candidates/.gitkeep
!data/c_src/caches/.gitkeep

data/shared_libs/archives/*
data/shared_libs/candidates/*
data/shared_libs/caches/*

!data/shared_libs/archives/.gitkeep
!data/shared_libs/candidates/.gitkeep
!data/shared_libs/caches/.gitkeep

# If switching to CMake or other build systems later, add their output directories here
# Examples: build/, cmake-build-debug/, etc.
# build/
# cmake-build-*/

# -------------------------------------------------------------------
# === FastAPI / Uvicorn Logs & Temporary Files ===
# Adjust as needed—ignore if you use log files or temp cache directories
*.log
*.tmp

# -------------------------------------------------------------------
# === IDE / Editor-Related Files ===
# Common config files for VSCode, PyCharm, etc.
.vscode/
.idea/
*.swp
*.swo

# -------------------------------------------------------------------
# === System-Level Ignores (Optional) ===
# macOS .DS_Store
.DS_Store

# Windows thumbnail files
Thumbs.db

# Other system cache files
desktop.ini
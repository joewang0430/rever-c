//
// Report section, shows below the game section, when generating report
//


interface ReportSectionProps {
    matchId: string;
};

export default function ReportSection({ matchId }: ReportSectionProps) {
    return (
        <>tbd</>
    )
}